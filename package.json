{
  "name": "pulse-ai",
  "version": "0.1.0",
  "description": "Agent interaction quality measurement — convergence, intent anchoring, decision quality",
  "bin": {
    "pulse": "./bin/pulse.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/**/*.test.js",
    "prepublishOnly": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
